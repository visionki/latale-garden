# 验证记录

最近验证：2026-09-18。环境：Windows 11 25H2，Build 26200，x64，.NET Framework 4.8.1；应用目标为 .NET Framework 4.8。

## v1.1.4 · Win11 临时区域方案（未发布）

- 主线从 v1.1.3 对应的稳定代码恢复开发，保留原临时全局切换、独立保护进程、取消/超时恢复与区域管理；不采用进程注入实验。
- 本轮 89 项检查通过：46 项启动与区域流程、13 项区域/系统限制界面、12 项更新界面、14 项更新协议、4 项单 EXE 分发。
- 新增检查确认：不支持的系统在备份、保护进程、区域写入及游戏启动之前拒绝繁体兼容；普通启动仍可使用；已有未完成事务仍能恢复。界面明确显示“普通启动”和不修复乱码的提示，不会悄悄保留兼容开关。
- 构建资源检查确认 EXE 仅包含界面、美术、图标及更新公钥，没有 GardenLocale DLL、原生启动组件或 Detours 资源。本机构建正确识别为支持 Win11 临时区域模式。
- 已渲染并检查 Win11 首页，单文件移动后界面及设置一致，用户设置文件摘要不变。
- 本轮未启动真实游戏、未实际修改系统区域；全局切换与恢复的历史实测见下文。用户已反馈原方案在 Win11 可正常运行，但本次没有新增长时间游玩验收。
- Win10 22H2 / Build 19045 的实际反馈是“游戏启动但仍乱码”；日志显示配置 ACP=950、新进程 GetACP=936。因此不再把 Win10 作为等待验证的平台，当前明确不支持其乱码修复。
- GitHub Release 与签名更新源保持已发布 v1.1.3，本地 v1.1.4 为未发布构建。以后发布时的更新清单最低 Build 为 22000。
- 本地构建 SHA-256：`05133a5311e0e6752813f5ba7396451340e2fee771c2c086d054dd034ff4a85e`。

以下为 v1.1.0–v1.1.3 的历史验证记录。

## v1.1.3 内置更新与项目动态

- 43 项启动与区域流程回归、10 项区域界面检查、12 项更新界面检查、4 项单文件检查通过。
- 14 项更新协议检查通过：签名错误、清单篡改、版本排序与降级拒绝、正式/测试通道、最低系统要求、下载地址、重复版本、大小与摘要、程序集版本、下载中断/取消、偏好迁移、公告时间/已读/链接和路径校验。
- 6 项真实安装检查通过：独立助手替换 EXE、真实新版 WPF 首帧确认、启动失败精确回退、篡改包拒绝、区域事务占用拒绝、文件占用保留旧版，以及用户游戏设置不变（最后一项单独检查设置摘要）。
- 真实替换使用同份源码生成的 9.0.0 测试构建，仅用于本地测试，没有发布该版本；启动失败使用立即退出的测试程序。文件名含中文和空格，使用 C 盘与 D 盘独立目录测试；备份/下载仍在当前用户的 C 盘数据目录。
- 替换成功、回退和正常失败后，目标目录均只留下原文件名的 EXE；副本备份保存在用户数据目录。未启动游戏、未写入系统区域。
- 签名后的本地更新清单与公告已通过生产解析代码检查。发布后，`test-published-update.ps1` 使用生产代码成功读取 GitHub 公网清单和公告并验签，下载的 EXE 与本机测试构建一致（SHA-256：`60e7bb1c62a668498630bfaa6b65ec6d2b836e98a54c39034b053f7e5a47ce10`）。
- 已渲染并检查首页、更新页面、下载完成状态、设置及关于页；设置页保存按钮固定显示。
- 本轮不覆盖真实 Win10、受保护目录的 UAC 提权、断电/磁盘故障、真实杀毒软件长期锁定或新版本全部业务功能。启动确认仅表示新版界面正常打开，不能替代功能验收。

## v1.1.2 应用图标更新

- 构建成功，10 项原生区域页面检查和 4 项单 EXE 分发检查通过，用户设置未被修改。
- 内嵌 ICO 包含 16、20、24、32、40、48、64、128、256 像素九种图像，每种图像均核对尺寸和真实透明通道。
- Windows 可从构建后的 EXE 提取新图标；WPF 窗口可加载内置 256 像素 PNG，实际 NotifyIcon 可加载并显示内置 32 像素图标，资源流关闭后仍有效。
- 已检查深浅两种背景下的多尺寸预览，无棋盘背景；单独复制 EXE 后仍能加载全部图形资源。
- 本版仅更新应用图标、版本和相关文档。字体检测、字体安装尚未加入，启动与区域恢复流程保持原实现。
- 本轮未启动游戏、修改系统区域或安装字体。Win10 实机及游戏内文字仍待用户验证。

## v1.1.1 文档与关于页更新

- 新版构建成功，现有 10 项原生区域页面检查与 4 项单 EXE 分发检查通过。
- 已启动并渲染首页、关于页和日志页，确认新增入口、作者、版本和反馈按钮完整显示；[关于页预览](docs/about.png)。
- 作者主页、项目主页和 GitHub Issues 链接均返回 HTTP 200。链接由用户点击后交给默认浏览器打开。
- README 改为用途、快速开始、常用功能和反馈在前，技术说明与构建在后；补充中文问题反馈表单。
- 此次为文档与界面更新，未执行真实区域修改。以下 43 项流程检查及实际区域切换记录来自 v1.1.0，对应的启动和区域处理逻辑保持不变。

## 自动流程与 Windows 检查

`test.ps1` 的 **43 项检查全部通过**，使用替代平台验证区域写入与故障分支，不执行真实区域切换或游戏启动。Windows 检查仅查询状态、验证互斥锁，或启动并回收自有的只休眠测试子进程。

- 保留 v1.0 的 22 项检查：启动等待、窗口稳定、取消/超时/界面退出、部分写入失败、恢复重试与读回、非简体原值、UTF-8 拒绝、进程身份、Windows Job、原子文件写入等。
- 新增 21 项区域检查：已完成且当前值一致的备份仍真实重新应用；手动选择成功后保留；失败回退到操作前值；明确恢复失败后继续恢复目标备份；重启辅助流程后保持正确恢复意图。
- 覆盖保护进程失败、操作前后取消/父进程退出、已有游戏及检查期间新开游戏、配置和备份过期、未知待恢复记录、UTF-8、不支持的区域、无效路径与损坏备份。
- 人工处理的记录以源文件摘要关联成功操作；未提交操作或已变化的历史文件不会被当作恢复完成。原记录保持不变。

当前回归逐项输出见 [TEST-RESULTS.txt](TEST-RESULTS.txt)。

## 原生区域页面检查

`test-ui.ps1` 的 **10 项检查全部通过**。直接加载发布 EXE 的 WPF 控件和区域显示逻辑，用不同状态检查按钮权限、提示及布局，不点击系统修改确认框、不执行系统写入。

覆盖：正常可操作、主要控件处于可见范围、无备份、游戏运行、其他区域事务占用、读取期间配置变化、UTF-8、配置与新进程读数不同、未完成历史记录、正在检测时禁止重复提交。

已启动并渲染真实首页、区域页和展开详情页，检查文字、入口与可滚动内容。图片是原生界面输出，不是设计模型。主要按钮在详情折叠时均可见，展开后详情可以滚动。

当前界面回归输出见 [UI-TEST-RESULTS.txt](UI-TEST-RESULTS.txt)；历史截图见 [首页](docs/launcher-preview.png)、[区域页](docs/region-management.png)。此次没有覆盖所有系统缩放与多显示器组合。

## 单 EXE 检查

`test-portable.ps1` 的 **4 项检查通过**：

1. 只复制 EXE 到含空格的新目录，嵌入的 WPF 界面和图片可加载。
2. 移动到第二个目录、从不同工作目录启动后，界面和保存的游戏目录渲染一致。
3. EXE 旁边和工作目录均无额外配置、DLL 或素材文件。
4. 当前用户设置文件的 SHA-256 不变。

测试截图写入脚本指定的结果目录。正常数据路径仍是 `%LOCALAPPDATA%\LaTaleGarden`。

## 本机真实区域写入

用户确认退出游戏后，在管理员权限下用构建出的 `LaTaleGarden.exe --region` 执行。每一步均等待工作进程及其独立恢复进程结束，再读取配置；测试记录放在独立测试目录，没有覆盖用户原来的启动备份。

| 检查 | 结果 |
| --- | --- |
| 对已完成、且与当前一致的旧版备份再次恢复 | 实际重新调用恢复，日志确认，不因已完成而跳过。 |
| 手动选择繁体台湾 | Default=0404，ACP/OEMCP=950，MACCP=10002，工作与保护进程退出后保持该值。 |
| 手动选择简体中国 | Default=0804，ACP/OEMCP=936，MACCP=10008，工作与保护进程退出后保持该值。 |
| 从繁体或简体重新应用原备份 | 回到测试前完整配置，原备份文件摘要不变。 |
| 未完成的明确恢复记录，测试父进程退出 | 生产独立恢复进程实际接管，恢复到指定备份，清除待恢复标记并提交恢复结果。 |
| 测试最终恢复 | 重新应用并核对原基线：zh-CN / 0804 / 936 / 936 / 10008。 |

另有独立基线保护进程监视整个实测，防止测试程序中途退出后留下繁体配置。本次测试结束时原值已全部核对；没有自动重启电脑，没有启动或修改官方游戏。

**实测观察**：配置切到 zh-TW 后，新检查进程 ANSI/OEM 为 950，但 `GetSystemDefaultLCID` 仍为 0804；切回后为 936/936/0804。说明“保存的配置”与不同 API 的读数可能暂时不一致。页面会提示这种差异，不将代码页读数解释为游戏内文字正常。

原始结果见 [REGION-LIVE-RESULTS.txt](REGION-LIVE-RESULTS.txt)。`test-region-live.ps1` 需要显式的 `-AllowSystemLocaleChanges`，不纳入普通自动检查。

## 已有功能的验证范围

v1.0 已完成本机设置保存、日志导出、现有管理员游戏进程识别以及 PowerShell 只读命令检查。v1.1 保留这些实现，并通过原流程回归检查。新区域页新增的检测数据随日志导出。

## 历史验证的剩余范围

- Win11 其他版本及不同 DPI/多显示器组合。Win10 后续实测无效，现已排除乱码修复支持。
- 真实临时切换 → 官方更新/登录 → 新客户端 → 自动恢复的完整游戏运行链。
- 游戏中文字、长时间游玩及游戏保护组件交互。
- 在 `Set-WinSystemLocale` 正在写入的瞬间强制结束辅助进程；本次实机故障检查使用持久化的未完成记录和被结束的只休眠测试父进程，再由生产保护进程实际恢复，不能等同于覆盖所有中断时机。
- 断电、系统崩溃、所有恢复进程同时被结束等场景。

区域管理在本机的切换和恢复已验证；不能据此推断其他系统或所有游戏版本的免重启修复效果。Release 的正式版状态不代表上述待验收项目已经完成。
