# 验证记录

日期：2026-09-17。环境：Windows 11 25H2，Build 26200，x64，.NET Framework 4.8.1；应用目标为 .NET Framework 4.8。

## 自动检查

22 项通过。流程检查使用替代平台与虚拟时间，**没有执行系统区域写入或启动游戏**。额外的 Windows 检查只读取进程身份、查询区域，或启动并回收自有的只休眠测试子进程。

1. 等待新客户端与稳定窗口后恢复，保留原日文区域而非固定简体。
2. 标准模式不捕获、不修改区域，也不启动恢复监护进程。
3. 准备前取消不修改区域。
4. 等待时取消会恢复。
5. 界面进程退出会触发恢复。
6. 超时会恢复。
7. 目录错误、已有游戏/启动器时，在区域操作前停止。
8. 独立恢复进程未就绪时不切换区域。
9. 区域切换部分失败会回滚，不启动官方启动器。
10. 官方启动器启动失败会回滚。
11. 恢复暂时失败会重试，并核对结果后才解除待恢复标记。
12. 持续恢复失败保留备份；重新载入备份后可再次恢复。
13. 恢复值核对不一致不能报告成功。
14. 单有进程不等同于窗口就绪；窗口消失会重置稳定等待。
15. UTF-8 配置在临时模式下被拒绝，不修改设置。
16. 无效恢复值在注册表操作前被拒绝。
17. PID 与创建时间共同匹配，错误创建时间被拒绝。
18. 路径空格和末尾反斜杠的参数引用正确。
19. Windows Job 关闭时会终止其自有测试子进程。
20. 能以低权限查询当前已运行的管理员 LaTale 客户端身份。
21. 损坏、空白或缺失恢复记录不能被当作恢复成功。
22. 状态文件被临时占用时，原子写入会重试并保留正确结果。

详细逐项输出见同目录 `TEST-RESULTS.txt`。可用 `test.ps1` 重跑。

## 单 EXE 分发检查

2026-09-17 使用不带 `.config`、DLL 或素材文件的新构建验证，`test-portable.ps1` 的 4 项检查通过：

1. 仅复制一个 EXE 到含空格的独立目录，可启动原生 WPF 窗口并渲染界面。
2. 关闭后移动该 EXE 到另一目录，从不同的工作目录再次启动，渲染结果完全一致，包括已保存的游戏路径。
3. 第一个 EXE 目录为空，第二个目录仅有 EXE，工作目录没有额外文件；预览产物写入测试脚本指定的结果目录。
4. 当前用户的设置文件校验值保持不变。

这组检查验证了无旁边依赖的界面启动与设置读取；它不替代真实区域切换及游戏内验证。运行时数据路径仍为 `%LOCALAPPDATA%\LaTaleGarden`。`.NET Framework 4.8` 目标、最低版本检查和 DPI 设置均位于 EXE 中。

## 本机原生界面

- WPF 窗口真实启动，左侧生图美术层与原生交互控件共同显示。
- 首页、设置页、日志页显示正常，主要按钮有可访问名称。
- 配置保存到 `%LOCALAPPDATA%\LaTaleGarden\settings.json`，读回参数与界面一致。
- 通过真实保存对话框导出日志，读回 UTF-8 中文文本正常。
- 当前管理员游戏进程保持运行；修复了 .NET Framework 的 `Process.HasExited` 在权限不足时产生的误判，改用 `PROCESS_QUERY_LIMITED_INFORMATION`。
- PowerShell 只读命令检查成功；使用无 profile、无交互、隐藏窗口、输出捕获、超时和 Windows Job。
- 检查前后注册表区域均为 zh-CN；ACP=936，OEMCP=936，MACCP=10008；当前进程 ACP=936。
- 没有更改官方游戏文件、没有终止或重启现有游戏，也没有执行临时区域切换。

## 尚未验收

- 真实 zh-TW 临时切换 → 官方启动器 → 新客户端 → 恢复的完整运行链。
- 进入游戏后的中文显示、长时间游玩、更新中断和游戏保护组件交互。
- Win10 实机、Win11 其他版本、不同 DPI/多显示器组合。
- 在真实区域写入期间强制结束辅助进程的端到端恢复。当前已验证替代平台的恢复分支和 Windows Job 行为，不能用它们替代这一实机测试。

因此交付物是已编译、已接上真实系统与游戏接口的首版，不能描述为“跨系统乱码修复已全部验证”。
